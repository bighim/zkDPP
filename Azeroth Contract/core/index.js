import compiler from './compiler.js';
import deploy from './deploy.js';
import deployToken from './deployToken.js';

const solcModules = {
    compiler,
    deploy,
    deployToken,
};

export default solcModules;
